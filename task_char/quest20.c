  /* Программа должна склеивать строку, которая уже есть в программе и строку, введенную пользователем. */

#include <stdio.h>
#include <string.h>

int main() {
/*   char str1[40] = "Hello, "; 
  char str2[20];    
  scanf("%s", str2);
  printf("%s", strcat(str1, str2)); */

  /*     char str[40] = "Hello, "; 
    scanf("%s", str + 7);
    printf("%s\n", str); */

  // printf("%s%s", "Hello, ", gets(stderr)); // no compilate


  return 0;
}
